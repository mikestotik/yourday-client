import { TasksTitlePipe } from './tasks-title.pipe';

describe('TasksTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TasksTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
