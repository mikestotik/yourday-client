import { Routes } from '@angular/router';
import { StatisticsComponent } from './statistics.component';

export const StatisticsRouting: Routes = [
  {
    path: '',
    component: StatisticsComponent
  }
];
